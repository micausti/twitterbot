import org.scalabridge.today
import org.scalatest.FlatSpec

/*

class SetSpec extends FlatSpec {
  "dayOfWeek method" should "get a calendar instance and determine the day of the week" in {
    val todayTest = new today.apply
    assert(todayTest.today(1) == "Sunday")
    assert(todayTest.today(2) == "Monday")
    assert(todayTest.today(3) == "Tuesday")
    assert(todayTest.today(4) == "Wednesday")
    assert(todayTest.today(5) == "Thursday")
    assert(todayTest.today(6) == "Friday")
    assert(todayTest.today(7) == "Saturday")
    }
  }

*/


